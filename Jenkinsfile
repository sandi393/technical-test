pipeline {
    agent any

    stages {
        stage('UnitTest') {
            steps {
                echo 'Ini adalah stage unit test'
            }
        }
        stage('Build') {
            steps {
                echo 'Ini adalah stage Build'
            }
        }
        stage('Deploy') {
            steps {
                echo 'Ini adalah stage Deploy'
            }
        }
    }
}

